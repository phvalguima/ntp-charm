name: NTPq Charm
summary: A charm that runs NTPq instead of Chrony
description: |
  A charm that runs NTPq instead of Chrony
tags:
  - ntp
series:
  - xenial
  - bionic
  - cosmic
subordinate: true
requires:
  container:
    interface: juju-info
    scope: container
